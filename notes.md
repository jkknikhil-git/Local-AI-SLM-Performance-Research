# Project Notes

Running observations, decisions, and findings logged here as the project progresses.

---

## Setup Notes

- Ollama installed on Windows, serving on `http://localhost:11434`
- All inference runs on CPU + system RAM (no CUDA, no GPU offload)
- Default quantization: Q4_K_M (Ollama default on pull)

## Observations

> To be filled in as benchmarks run.
